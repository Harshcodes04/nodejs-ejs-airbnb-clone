<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add home to airbnb</title>
    <link href="/output.css" rel="stylesheet" />
  </head>
  <body>
    <%- include('partials/nav')%>
    <main class="container mx-auto max-w-2xl mt-10 px-4">
      <div class="bg-white shadow-lg rounded-lg p-8">
        <h1 class="text-3xl font-bold text-gray-800 mb-8">
          Register your home
        </h1>
        <form action="/host/add-home" method="POST" class="space-y-6">
          <div class="flex flex-col">
            <label
              for="houseName"
              class="text-sm font-medium text-gray-700 mb-2"
            >
              House Name
            </label>
            <input
              type="text"
              id="houseName"
              name="houseName"
              placeholder="Enter house name"
              required
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent transition duration-200"
            />
          </div>
          <div class="flex flex-col">
            <label for="address" class="text-sm font-medium text-gray-700 mb-2">
              Address
            </label>
            <input
              type="text"
              id="address"
              name="address"
              placeholder="Enter house address"
              required
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent transition duration-200"
            />
          </div>
          <div class="flex flex-col">
            <label for="price" class="text-sm font-medium text-gray-700 mb-2">
              Price per Night (INR)
            </label>
            <input
              type="number"
              id="price"
              name="price"
              placeholder="Enter price per night"
              required
              min="0"
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent transition duration-200"
            />
          </div>
          <div class="flex flex-col">
            <label for="rating" class="text-sm font-medium text-gray-700 mb-2">
              Rating (1-5)
            </label>
            <input
              type="number"
              id="rating"
              name="rating"
              placeholder="Enter rating (1 to 5)"
              min="1"
              max="5"
              step="1"
              required
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent transition duration-200"
            />
          </div>
          <div class="flex flex-col">
            <label for="rating" class="text-sm font-medium text-gray-700 mb-2">
              Rating (1-5)
            </label>
            <input
              type="url"
              id="photoUrl"
              name="photoUrl"
              placeholder="Enter photo URL"
              required
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent transition duration-200"
            />
          </div>
          <button
            type="submit"
            class="w-full bg-red-500 hover:bg-red-600 text-white font-semibold py-3 px-6 rounded-lg transition duration-200 shadow-md hover:shadow-lg"
          >
            Add Home
          </button>
        </form>
      </div>
    </main>
  </body>
</html>
